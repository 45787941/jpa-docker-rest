CREATE TABLE PERSONNE (PRENOM VARCHAR(255) NOT NULL, NOM VARCHAR(255) NOT NULL, AGE INTEGER, DOG_IDC INTEGER, PRIMARY KEY (PRENOM, NOM))
CREATE TABLE CHIEN (IDC INTEGER NOT NULL, NOM VARCHAR(255), RACE VARCHAR(255), CHENIL_IDCHENIL INTEGER, PRIMARY KEY (IDC))
CREATE TABLE CHENIL (IDCHENIL INTEGER NOT NULL, NOMCHENIL VARCHAR(255), PRIMARY KEY (IDCHENIL))
CREATE TABLE PATHOLOGIE (IDPATHOLOGIE INTEGER NOT NULL, DESCRIPTION VARCHAR(255), NOMPATH VARCHAR(255), PRIMARY KEY (IDPATHOLOGIE))
CREATE TABLE CHIEN_PATHOLOGIE (listeChiens_IDC INTEGER NOT NULL, listePathologie_IDPATHOLOGIE INTEGER NOT NULL, PRIMARY KEY (listeChiens_IDC, listePathologie_IDPATHOLOGIE))
ALTER TABLE PERSONNE ADD CONSTRAINT FK_PERSONNE_DOG_IDC FOREIGN KEY (DOG_IDC) REFERENCES CHIEN (IDC)
ALTER TABLE CHIEN ADD CONSTRAINT FK_CHIEN_CHENIL_IDCHENIL FOREIGN KEY (CHENIL_IDCHENIL) REFERENCES CHENIL (IDCHENIL)
ALTER TABLE CHIEN_PATHOLOGIE ADD CONSTRAINT FK_CHIEN_PATHOLOGIE_listePathologie_IDPATHOLOGIE FOREIGN KEY (listePathologie_IDPATHOLOGIE) REFERENCES PATHOLOGIE (IDPATHOLOGIE)
ALTER TABLE CHIEN_PATHOLOGIE ADD CONSTRAINT FK_CHIEN_PATHOLOGIE_listeChiens_IDC FOREIGN KEY (listeChiens_IDC) REFERENCES CHIEN (IDC)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN_TABLE', 0)
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
